<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>ko</string>
	<key>CFBundleDisplayName</key>
	<string>$(PK_DISPLAY_NAME)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>XPC!</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<!-- docs/06 §7a: the entitlement gate reads this, and it must agree with the app's or
	     a STAGING build would ship a read-only widget beside an interactive screen. Both
	     come from the same PK_APP_ENV in Config/{Dev,Staging,Prod}.xcconfig. -->
	<key>PKAppEnvironment</key>
	<string>$(PK_APP_ENV)</string>
	<!-- docs/06 §6: the container the app and this extension share. Per configuration, so
	     DEV/STAGING/PROD cannot overwrite one another's active parking. -->
	<key>PKAppGroupIdentifier</key>
	<string>$(PK_APP_GROUP)</string>
	<key>NSExtension</key>
	<dict>
		<key>NSExtensionPointIdentifier</key>
		<string>com.apple.widgetkit-extension</string>
	</dict>
</dict>
</plist>

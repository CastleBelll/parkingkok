# Fail loudly when the active Node runtime does not match package.json engines.
# Cloud Functions 2nd gen deploys on Node 22; this host defaults to v24, and
# without engine-strict npm runs the wrong runtime silently green (G-1 QA #1).
engine-strict=true

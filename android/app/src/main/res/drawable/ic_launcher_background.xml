<?xml version="1.0" encoding="utf-8"?>
<!--
  The launcher background is the app's own surface colour, not a brand slab. The mark is
  the blue pin in the foreground layer; a blue field behind a blue pin would erase it,
  and a coloured background is the kind of decoration CLAUDE.md's design harness rules
  out. Literal here rather than a token because a launcher drawable is resolved by the
  launcher process, outside the app's theme.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M0,0h108v108h-108z" />
</vector>

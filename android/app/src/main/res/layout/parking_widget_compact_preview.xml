<?xml version="1.0" encoding="utf-8"?>
<!--
  Picker art only (docs/06 §7a Sizes). Never inflated on a home screen — the widget itself
  is Glance; this exists because a 2x2 entry showing only the app icon tells the user
  nothing about what they are about to place.

  Colours come from the host's theme attributes rather than from values of their own:
  docs/10_DESIGN_UX_SPEC.md §2 keeps every colour literal in `theme/Color.kt`, and the
  picker is the launcher's surface, not ours.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="12dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/home_active_title"
        android:textColor="?android:attr/textColorSecondary"
        android:textSize="12sp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_preview_floor"
        android:textColor="?android:attr/textColorPrimary"
        android:textSize="34sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_preview_elapsed"
        android:textColor="?android:attr/textColorSecondary"
        android:textSize="12sp" />
</LinearLayout>
